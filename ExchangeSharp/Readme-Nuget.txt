When deploying a new nuget package, ensure that the assembly info versions are changed, along with the version in the ExchangeSharp.csproj under VersionPrefix.

Then turn on nuget package building under package tab in project settings.